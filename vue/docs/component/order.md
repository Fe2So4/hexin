#### 订单新增/修改组件(Order)
#### 说明
组件用于订单的修改和新增；
组件加载时根据是否匹配到传递的id；
确认是修改订单还是新建订单
#### 依赖模块


#### 代码示范
```vue

```

#### 参数说明
+ 内部参数(data)

| 参数名| 参数说明| 何时改变|类型|可选值|默认值|
| - |:-:|:-:|:-:|:-:|:-:|
| order| 订单对象 | 页面加载时 |Object|-|-|

+ 外部参数(props)

| 参数名| 参数说明| 详述|类型|可选值|默认值|
| - |:-:|:-:|:-:|:-:|:-:|
|id|order的id|用于从全局订单列表中查询到对应的订单|int|非必需|无|

+ 事件(events)

| 事件名| 说明| 回调参数 |
| - |:-:|:-:|
|- |- |- |

+ 函数(methods)

| 函数名名| 说明| 参数 |
| - |:-:|:-:|
|addOrder |新增order的函数 |order |
|updateOrder |编辑order的函数 |order |

#### 变更日志

2017-11-20 -组件创建 by syz。