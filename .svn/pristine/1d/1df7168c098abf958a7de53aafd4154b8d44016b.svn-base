import { searchInsCom } from '@/api/silverInsurAgent_mgr/silverDitch_import.js'
export const silverDitchImport = {
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
    searchSilverInsCom ({commit}, query) {
      return searchInsCom(query).then(resp => {
        return resp.data
      })
    }
  }
}
