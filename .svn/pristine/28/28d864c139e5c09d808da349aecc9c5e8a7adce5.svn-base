import { queryPolicyById } from '@/api/lifeInsur_mgr/lifeInsur_mgr.js'
// import { getAllsilverCom, getAllInsOrg } from '@/api/dailogApi/dailogApi.js'
export const billMod = {
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
    queryPolicyById_Mod ({commit}, query) {
      return queryPolicyById(query).then(resp => {
        return resp.data
      })
    }
  }
}
