import {queryRate} from '@/api/silverInsurCom_mgr/agentList.js'

export const silverInsurComMgr = {
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
    queryRate ({commit}, query) {
      return queryRate(query)
    }
  }
}
